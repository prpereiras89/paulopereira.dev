services:
  app:
    build:
      context: .
    container_name: ppereiradev-site
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    volumes:
      - ./my-site:/my-site
    ports:
      - 3000:3000
